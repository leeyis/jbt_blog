# Django核心框架
Django>=5.2.2

# 数据库适配器（Alpine Linux需要使用源码编译版本）
psycopg2>=2.9.0

# Markdown编辑器和相关依赖
django-mdeditor>=0.1.20

# 管理界面美化
django-admin-interface>=0.30.0
django-colorfield>=0.14.0

# Markdown处理和代码高亮
markdown>=3.8
Pygments>=2.19.1

# 可选依赖（仅在需要数据迁移时使用）
# html2text>=2025.4.15

# Django核心依赖（通常自动安装，但明确列出确保兼容性）
asgiref>=3.8.1
sqlparse>=0.5.3